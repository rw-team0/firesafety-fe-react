# 이 파일을 복사해 .env.local로 만들고 실제 값을 채우세요. .env.local 자체는 Git에 커밋하지 않습니다.

# 개발 API proxy 대상(백엔드 서버 주소)
VITE_DEV_API_PROXY_TARGET=http://localhost:8080

# 개발 WebSocket proxy 대상
VITE_DEV_WS_PROXY_TARGET=http://localhost:8080
